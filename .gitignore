# Variáveis de ambiente locais — nunca commitar segredos, tokens, chaves, cookies, sessões ou credenciais
.env
.env.*
!.env.example

node_modules/
dist/

# Configuração local de ferramenta (Claude Code) — não faz parte do site
.claude/

# tools/ (instagram, pagespeed, tracking, ...) migrou integralmente para
# trevodigitalconversoes/trevo-ops (privado) em 2026-08-08 -- ver
# CLAUDE.md/AGENTS.md, item 13. Nao recriar tools/<algo>/ neste repo.
/evidencias/
