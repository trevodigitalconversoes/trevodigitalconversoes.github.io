# Variáveis de ambiente locais — nunca commitar segredos, tokens, chaves, cookies, sessões ou credenciais
.env
.env.*

node_modules/
dist/

# Configuração local de ferramenta de desenvolvimento — não faz parte do site
.claude/
